{
  "name": "nova-code",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop AI coding assistant — multi-provider, skills, plugins, Telegram",
  "workspaces": [
    "packages/shared",
    "packages/engine",
    "packages/desktop"
  ],
  "scripts": {
    "dev": "bun run --filter @nova/desktop dev",
    "build": "bun run --filter @nova/shared build && bun run --filter @nova/engine build && bun run --filter @nova/desktop build",
    "build:engine": "bun run --filter @nova/shared build && bun run --filter @nova/engine build",
    "lint": "bunx biome check --write .",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@tailwindcss/postcss": "^4.3.1",
    "@types/node": "^25.9.3",
    "bun-types": "^1.3.14",
    "concurrently": "^10.0.3",
    "nanoid": "^5.1.11",
    "typescript": "5.8.3"
  },
  "trustedDependencies": [
    "node-pty"
  ]
}
